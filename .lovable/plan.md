

Add `marginLeft: '100px'` to the image's inline style to shift it 100px to the right.

**Change in `src/components/HeroSection.tsx` (line 56):**
```tsx
style={{ width: '900px', height: 'auto', marginLeft: '100px' }}
```

